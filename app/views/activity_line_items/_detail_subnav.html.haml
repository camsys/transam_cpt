= sub_nav_tag({}) do
  = sub_nav_elem_tag do
    = link_to capital_project_path(@project) do
      %i.fa.fa-angle-left= " Back"
    = sub_nav_elem_tag do
      = link_to edit_capital_project_activity_line_item_path(@project, @activity_line_item) do
        %i.fa.fa-edit= " Update"
    = sub_nav_elem_tag do
      = link_to capital_project_activity_line_item_path(@project, @activity_line_item), :method => :delete, :data => {:confirm => "Are you sure?. The action cannot be undone!."}  do
        %i.fa.fa-times= " Remove"
    = sub_nav_elem_tag do
      = link_to new_capital_project_activity_line_item_path  do
        %i.fa.fa-plus= " Create New"

.row
  .col-md-12{:style => 'height:200px;overflow-y:auto;'}
    - if @activity_line_item.assets.empty?
      %p There are no assets for this activity line item.
    - else
      = render :partial => 'assets_datatable', :locals => {:assets => @activity_line_item.assets}

.row
  .col-md-12
    = link_to "Add Assets", "#", :class => "btn btn-primary"
  
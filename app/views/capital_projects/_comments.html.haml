.row
  .col-md-12{:style => 'height:200px;overflow-y:auto;'}
    - if @project.comments.empty?
      %p There are no comments for this project.
    - else
      = render :partial => 'shared/comments', :locals => {:comments => @project.comments}

- if can? :update, @project
  .row
    .col-md-12
      %fieldset
        %legend Add Comment
        = form_for [@project, Comment.new], :role => 'form' do |f|
          .form-group
            = f.text_area :comment, :class => 'form-control', :placeholder => 'Enter a new comment...', :required => true, :minlength => 10
          = f.submit :class => 'btn btn-primary'
  
.row
  .col-sm-12
    - if @project.comments.empty?
      %p There are no comments for this project.
    - else
      = render :partial => 'shared/comments', :locals => {:comments => @project.comments}

- if can? :add_comment, @project
  %button.btn.btn-primary{:data => {:toggle => "modal", :target => "#comment_modal"}}
    %i.fa.fa-plus
    = " Add Comment"

.modal.fade#comment_modal{:tabindex => -1, :role => 'dialog', :aria => {:hidden => true}}
  .modal-dialog
    .modal-content
      .modal-header
        %button.close{:data => {:dismiss => "modal"}}
          x
        %h4.modal-title
          Add Comment
      .modal-body
        = render 'comment_form'

:ruby
  rep = CapitalPlanSummaryReport.new
  @data = rep.get_data(@organization_list, {})
  
.row
  - @data[:data].each do |row|
    = render :partial => 'capital_projects/capital_plan_summary_detail', :locals => {:year => row[0], :num_replace => row[1], :num_rehab => row[2], :cost => row[3]}
    
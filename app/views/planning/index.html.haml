= render 'user_activity_line_item_filters/form_panel'

.row
  .col-sm-12
    .page-header
      .btn-toolbar.pull-right{:style => 'margin-top: 7px;'}
        = render 'planning_actions'
      %h2 Project Planning

.row
  .col-sm-12
    #plan_view
      = render 'plan_view'

.modal.fade#form-modal{:tabindex => -1, :role => 'dialog', :aria => {:hidden => true}}

:javascript
  $(function() {
    $('.transam-tooltip').tooltip();
  });
  

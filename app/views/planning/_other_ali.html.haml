- draggable_class = can?(:update, ali) || can?(:update_cost, ali)  ? "draggable" : ""
.panel.panel-other{:class => draggable_class, :data => {:url => ali_action_planning_index_path, :year => ali.fy_year, :ali => ali.object_key, :action => PlanningController::ALI_MOVE_YEAR_ACTION, :project => ali.capital_project.object_key}}
  .panel-body
    = render :partial => 'planning/ali', :locals => {:project => project, :ali => ali}
